import { ShieldCheck } from 'lucide-react'

export default function Footer() {
  return <footer className="site-footer"><div className="page-width"><span className="brand-footer"><ShieldCheck size={16} /> ClearCart</span><p>Protected checkout · Exact terms before payment</p><span>Explicit consent at every commitment</span></div></footer>
}
